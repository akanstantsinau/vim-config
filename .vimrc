#hilight color syntax
syntax on

#Show line numbers
set nu

#Use spaces instead of tabs. Use 2 space character per one tab.
set tabstop=2
set shiftwidth=2
set expandtab

#'autoindent' does nothing more than copy the indentation from the previous line, when starting a new line.
set autoindent

#Scrolling in Mac's iTerm
set mouse=a

set encoding=utf-8

